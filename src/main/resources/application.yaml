spring:
  application:
    name: requisiciones
  datasource:
    url: jdbc:mysql://localhost:3308/s_requisiciones?createDatabaseIfNotExist=true
    username: root
    password: simecdev
    hikari:
      initialization-fail-timeout: 360
      connection-timeout: 300
  jpa:
    hibernate:
      # ddl-auto: $SPRING_JPA_HIBERNATE_DDL_AUTO
      ddl-auto: update
    show-sql: false
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect